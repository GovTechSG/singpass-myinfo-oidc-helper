import { EnumUtils } from "../../../util/EnumUtils";

export enum {{enumName}} {
	{{#each enumEntries}}
	"{{this.key}}" = "{{this.value}}",
	{{/each}}
}

enum {{enumName}}Mapping {
	{{#each enumEntries}}
	"{{this.key}}" = "{{this.desc}}",
	{{/each}}
}

type {{enumName}}Desc = `${ {{enumName}}Mapping }`;

export namespace {{enumName}} {
	export const fn = {
		keys: EnumUtils.keysFunc<{{enumName}}>({{enumName}}),
		values: EnumUtils.valuesFunc<{{enumName}}>({{enumName}}),
		toEnumKey: EnumUtils.toEnumKeyFunc<{{enumName}}>({{enumName}}),
		toEnumValue: EnumUtils.toEnumValueFunc<{{enumName}}>({{enumName}}),
		toEnumDesc: EnumUtils.toEnumDescFunc<{{enumName}}, {{enumName}}Desc>({{enumName}}, {{enumName}}Mapping),
	};
}
